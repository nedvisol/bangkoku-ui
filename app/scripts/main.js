'use strict';

define([
    'app'
    /*
     * Since facebook access is restricted not working in local
     * When publishing uncomment it
     */
    /*,'components/fb'*/
  ],
  function(App) {
    App.start();
  });
